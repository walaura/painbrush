{
  "type": "module",
  "@parcel/resolver-default": {
    "packageExports": true
  },
  "scripts": {
    "start": "rm -rf dist .parcel-cache && parcel serve ./src/index.html",
    "build": "rm -rf dist .parcel-cache && parcel build ./src/index.html"
  },
  "dependencies": {
    "@types/node": "^25.6.0",
    "painbrush": "file:./../painbrush",
    "parcel": "^2.16.4"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "path-browserify": "^1.0.1",
    "process": "^0.11.10"
  }
}
